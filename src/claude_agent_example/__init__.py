"""Claude Agent SDK example - production-shaped multi-tool agent.

Default backend is a deterministic stub so the kit runs anywhere without
keys. Set CLAUDE_AGENT_PROVIDER=claude (with ANTHROPIC_API_KEY set) to route
to the real Claude API.
"""
__version__ = "1.0.0"
